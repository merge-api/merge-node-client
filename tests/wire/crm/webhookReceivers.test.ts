// This file was auto-generated by Fern from our API Definition.

import { MergeClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("WebhookReceiversClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new MergeClient({
            maxRetries: 0,
            apiKey: "test",
            accountToken: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = [{ event: "event", is_active: true, key: "key" }];
        server
            .mockEndpoint()
            .get("/crm/v1/webhook-receivers")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.crm.webhookReceivers.list();
        expect(response).toEqual([
            {
                event: "event",
                isActive: true,
                key: "key",
            },
        ]);
    });

    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new MergeClient({
            maxRetries: 0,
            apiKey: "test",
            accountToken: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { event: "event", is_active: true };
        const rawResponseBody = { event: "event", is_active: true, key: "key" };
        server
            .mockEndpoint()
            .post("/crm/v1/webhook-receivers")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.crm.webhookReceivers.create({
            event: "event",
            isActive: true,
        });
        expect(response).toEqual({
            event: "event",
            isActive: true,
            key: "key",
        });
    });
});
