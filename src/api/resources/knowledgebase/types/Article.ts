// This file was auto-generated by Fern from our API Definition.

import type * as Merge from "../../../index";

/**
 * # The Article Object
 * ### Description
 * The `Article` object is used to represent a form of content in the knowledge base, such as an article or page.
 * ### Usage Example
 * Fetch from the `GET /api/knowledgebase/v1/articles` endpoint and view their articles.
 */
export interface Article {
    id?: string;
    /** The third-party API ID of the matching object. */
    remoteId?: string;
    /** The datetime that this object was created by Merge. */
    createdAt?: Date;
    /** The datetime that this object was modified by Merge. */
    modifiedAt?: Date;
    /** The title of the article. */
    title?: string;
    /** Description of the article. */
    description?: string;
    /** The user who created the article. */
    author?: Merge.knowledgebase.ArticleAuthor;
    /** The user to last update the article. */
    lastEditedBy?: Merge.knowledgebase.ArticleLastEditedBy;
    /**
     * The visibility of the article.
     *
     * * `PUBLIC` - PUBLIC
     * * `INTERNAL` - INTERNAL
     * * `RESTRICTED` - RESTRICTED
     */
    visibility?: Merge.knowledgebase.ArticleVisibility;
    /** The presigned S3 URL to fetch article content. */
    articleContentDownloadUrl?: string;
    /** The SHA256 checksum of the article content. */
    checksum?: string;
    /** The URL to the webpage of the article. */
    articleUrl?: string;
    /**
     * The status of the article.
     *
     * * `DRAFT` - DRAFT
     * * `PUBLISHED` - PUBLISHED
     * * `ARCHIVED` - ARCHIVED
     * * `TRASH` - TRASH
     */
    status?: Merge.knowledgebase.ArticleStatus;
    /**
     * The type of the article.
     *
     * * `PAGE` - PAGE
     * * `BLOG_POST` - BLOG_POST
     * * `SMART_LINK` - SMART_LINK
     */
    type?: Merge.knowledgebase.ArticleType;
    /** When the third party's article was created. */
    remoteCreatedAt?: Date;
    /** When the third party's article was updated. */
    remoteUpdatedAt?: Date;
    /** The parent article an article is nested within. */
    parentArticle?: Merge.knowledgebase.ArticleParentArticle;
    /** The parent container an article is nested within. */
    parentContainer?: Merge.knowledgebase.ArticleParentContainer;
    /** The top-level container in the hierarchy that holds this article. This will reference a container object that will typically be a SPACE or WORKSPACE type. */
    rootContainer?: Merge.knowledgebase.ArticleRootContainer;
    permissions?: Merge.knowledgebase.ArticlePermissionsItem[];
    attachments?: (Merge.knowledgebase.ArticleAttachmentsItem | undefined)[];
    /** Indicates whether or not this object has been deleted in the third party platform. Full coverage deletion detection is a premium add-on. Native deletion detection is offered for free with limited coverage. [Learn more](https://docs.merge.dev/integrations/hris/supported-features/). */
    remoteWasDeleted?: boolean;
    fieldMappings?: Record<string, unknown>;
    remoteData?: Merge.knowledgebase.RemoteData[];
}
