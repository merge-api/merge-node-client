/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Merge from "../../../index";

/**
 * # The Container Object
 * ### Description
 * The Container object is used to represent a grouping of articles in the knowledge base. This can include Spaces, Folders, Databases, etc.
 * ### Usage Example
 * Fetch from the `GET /api/knowledgebase/v1/containers` endpoint and view their containers.
 */
export interface Container {
    id?: string;
    /** The third-party API ID of the matching object. */
    remoteId?: string;
    /** The datetime that this object was created by Merge. */
    createdAt?: Date;
    /** The datetime that this object was modified by Merge. */
    modifiedAt?: Date;
    /** Name of the container. */
    name?: string;
    /** Description of the container. */
    description?: string;
    /**
     * The container's status.
     *
     * * `DRAFT` - DRAFT
     * * `PUBLISHED` - PUBLISHED
     * * `ARCHIVED` - ARCHIVED
     * * `TRASH` - TRASH
     */
    status?: Merge.knowledgebase.ContainerStatus;
    /** The URL to the webpage of the container. */
    containerUrl?: string;
    /**
     * The container's type.
     *
     * * `FOLDER` - FOLDER
     * * `SPACE` - SPACE
     * * `COLLECTION` - COLLECTION
     * * `SECTION` - SECTION
     * * `CATEGORY` - CATEGORY
     * * `DATABASE` - DATABASE
     */
    type?: Merge.knowledgebase.ContainerType;
    /**
     * The container's visibility.
     *
     * * `PUBLIC` - PUBLIC
     * * `INTERNAL` - INTERNAL
     * * `RESTRICTED` - RESTRICTED
     */
    visibility?: Merge.knowledgebase.ContainerVisibility;
    /** When the third party's container was created. */
    remoteCreatedAt?: Date;
    /** When the third party's container was updated. */
    remoteUpdatedAt?: Date;
    /** The parent article a container is nested within. */
    parentArticle?: string;
    /** The parent container a container is nested within. */
    parentContainer?: string;
    permissions?: Merge.knowledgebase.ContainerPermissionsItem[];
    /** Indicates whether or not this object has been deleted in the third party platform. Full coverage deletion detection is a premium add-on. Native deletion detection is offered for free with limited coverage. [Learn more](https://docs.merge.dev/integrations/hris/supported-features/). */
    remoteWasDeleted?: boolean;
    fieldMappings?: Record<string, unknown>;
    remoteData?: Merge.knowledgebase.RemoteData[];
}
