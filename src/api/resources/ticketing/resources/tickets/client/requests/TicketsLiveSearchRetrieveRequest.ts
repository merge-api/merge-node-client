// This file was auto-generated by Fern from our API Definition.

import type * as Merge from "../../../../../../index";

export interface TicketsLiveSearchRetrieveRequest {
    /** Filter tickets by assignee IDs (comma-separated) */
    assigneeIds?: string;
    /** Filter tickets by assignee names (comma-separated) */
    assignees?: string;
    /** Filter tickets by collection IDs (comma-separated) */
    collectionIds?: string;
    /** Filter tickets by collection names (comma-separated) */
    collections?: string;
    /** Indicates whether or not this object has been deleted in the third party platform. Full coverage deletion detection is a premium add-on. Native deletion detection is offered for free with limited coverage. [Learn more](https://docs.merge.dev/integrations/hris/supported-features/). */
    includeDeletedData?: boolean;
    /** Whether to include all remote fields, including fields that Merge did not map to common models, in a normalized format. */
    includeRemoteFields?: boolean;
    /** Whether to include shell records. Shell records are empty records (they may contain some metadata but all other fields are null). */
    includeShellData?: boolean;
    /** Filter tickets by name/title */
    name?: string;
    /** Pagination cursor for remote data */
    remoteCursor?: string;
    /** Deprecated. Use show_enum_origins. */
    remoteFields?: Merge.ticketing.TicketsLiveSearchRetrieveRequestRemoteFields;
    /** A comma separated list of enum field names for which you'd like the original values to be returned, instead of Merge's normalized enum values. [Learn more](https://help.merge.dev/en/articles/8950958-show_enum_origins-query-parameter) */
    showEnumOrigins?: Merge.ticketing.TicketsLiveSearchRetrieveRequestShowEnumOrigins;
    /** Filter tickets by status */
    status?: string;
    /** Filter tickets by URL */
    ticketUrl?: string;
}
